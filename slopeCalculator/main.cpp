#include "slope.h"
#include <iostream>
#include <string>

using namespace std;

int main() {
    Slope slope();
    std::string y2, y1, x2, x1;
    std::cout << "Enter y2: ";
    std::getline(std::cin, y2);
    std::cout << "Enter y2: ";
    std::getline(std::cin, y1);
    std::cout << "Enter y2: ";
    std::getline(std::cin, x2);
    std::cout << "Enter y2: ";
    std::getline(std::cin, x1);
    //std::cout << "The slope is " << slope.calculateSlope(y2, y1, x2, x1) << "\n";
    return 0;
}